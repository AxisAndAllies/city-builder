{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "@types/fabric": "^4.2.6",
    "fabric": "^4.5.0",
    "fast-vector": "^2.3.3",
    "short-animal-id": "^3.0.2"
  },
  "devDependencies": {
    "@snowpack/plugin-typescript": "^1.2.1",
    "prettier": "^2.3.1",
    "snowpack": "^3.5.5",
    "typescript": "^4.3.2"
  }
}
